# Dependencias reales de Caja Tiquipaya (auditadas por import en todo el repo:
# raíz, v3/, tests/, tests_v3/, parity_v3/). El motor contable y v3/*.py no
# usan nada más que la librería estándar de Python + estos dos paquetes.
# El acceso a Google Drive vive en n8n (nodos googleDrive), nunca en Python.
openpyxl>=3.1,<4
pytest>=8,<10
